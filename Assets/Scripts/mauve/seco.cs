using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class seco : MonoBehaviour
{
    [SerializeField] firs boca;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(boca.barl);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
